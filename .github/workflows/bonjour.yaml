name: Exemple Bonjour

on:
  push:
    branches: [ "main" ]

jobs:
  dire-bonjour:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code source
        uses: actions/checkout@v4

      - name: Afficher Bonjour
        run: echo "Bonjour depuis GitHub Actions !"

      - name: Lister les fichiers du runner
        run: ls -la


